import {
	prepareSettings,
	settingsProp
} from './prepareSettings';
import {
	resolvers
} from './resolvers';
import {
	formTemplateReducer
} from './templates-store';

/**
 * Export all state-related functions
 */
export default {
	prepareSettings,
	settingsProp,
	resolvers,
	formTemplateReducer
};